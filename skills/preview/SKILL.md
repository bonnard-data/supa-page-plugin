---
description: Open the preview URL for the current site (renders from source/)
allowed-tools: Bash
model: haiku
---

Open the staged-source preview URL in the browser.

!`bash ${CLAUDE_PLUGIN_ROOT}/scripts/preview.sh`

If the browser didn't open (printout starts with "Preview:" only), tell the user to click the URL above.
